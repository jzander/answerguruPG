<div class="container">
  <div class="answerRateBlock">

<%= form_for @decision do |f| %>
  <ul class="listOne">
  <%= f.fields_for :answers do |ans_builder| %>
      <li class="answertxt">For <%= ans_builder.object.text %><br />

      <span class="ratetxt">Rate the criteria: </span>

      <%= ans_builder.fields_for :criteria_answers do |crit_builder| %>
        <li class="criteriatxt">Rate <span class="criTxtt">
          <%= crit_builder.object.criterion.text %></span>
          <%#= crit_builder.text_field :rating %>
          <%= crit_builder.select :rating, [["<< select, bitch >>", nil]] + (1..10).map {|n| [n,n]} %>
        </li>
    <% end %>
    </li>
  <% end %>
  
  </ul> <br />
      
  <%= f.submit :class => "btn btn-default btn-md btn-bluer text-center", value: "calculate" %>      
<% end %>

</div>

  <%#= p.submit :class => "btn btn-default btn-md btn-bluer" %>


    <%#= form_for [@decision, @answer] do |q| %>
</div>

<br /><br />